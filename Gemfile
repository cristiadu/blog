# Inherit shared dependencies
eval_gemfile "Gemfile.base"

# Main blog specific dependencies (not included in github-pages)
group :jekyll_plugins do
  gem "jekyll-seo-tag"  # Enhanced SEO (different from jekyll-seo-tag)
end

# Blog-specific dependencies (if any)
# Add any dependencies specific to the main blog here.
